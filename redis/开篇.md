Q：为了保证数据的可靠性，Redis需要再磁盘上读写AOF和RDB，但在高并发场景里，这就会直接带来两个新问题：一个是写AOF和RDB会造成Redis性能抖动，另一个是Redis集群数据同步和实例恢复时，读RDB比较慢，限制了同步和恢复速度。  
A：使用非易失内存NVM，它既能保证高速的读写，又能快速持久化数据。
  
使用Redis的坑，总体来说集中在4个方面
- CPU使用上的坑，例如数据结构的复杂度、跨CPU核的访问；
- 内存使用上的坑，例如主从同步和AOF的内存竞争；
- 存储=持久化上的坑，例如在SSD上做快照的性能抖动；
- 网络通信上的坑，例如多实例时的异常网络丢包。

在学习Redis时，需要注意一个误区。那就是，**只关注零散的技术点，没有建立起一套完整的知识框架，缺乏系统观**，但是，系统观是非常重要的。拥有了系统观，就意味着可以有依据、有章法地定位和解决问题。  
如何高效地形成系统观呢？简单地来说，我们希望花很少的时间掌握更丰富的知识和经验，解决更多的问题。只要抓住主线，在自己的脑海中绘制一幅Redis全景知识图。  
那么，所谓的Redis知识全景图都包括什么呢？简单来说，就是“两大维度，三大主线”。  
![alt aaa](/Users/guoyh/other/file/study/redis/redis问题画像.jpg)

![alt 属性文本](图片地址)